// What will the following code log to the console and why? Don't run it until you have tried to answer.

let myWords = ["Hello"];
myWords.push("Goodbye");

console.log(myWords);

// It will log an array with ['Hello', 'Goodbye'] in it. Arrays are mutable and the push method adds a new element into an array.
