// What will the following code log to the console and why? Don't run it until you have tried to answer.

let myWords = ["Hello"];
myWords.concat(["Goodbye"]);

console.log(myWords);

// It will log ['Hello']. The array concat method returns a new array and does not mutate the original one.
